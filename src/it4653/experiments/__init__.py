"""Standalone experiment and analysis scripts.

Modules:
    latent_space       : Visualize 2D latent encodings + linear interpolation
    anomaly_detection  : Out-of-distribution detection via reconstruction error
    interpolation      : Side-by-side AE vs VAE interpolation
    compare_models     : Quantitative comparison (FID table + image grids)
"""
