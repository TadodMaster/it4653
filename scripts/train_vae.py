#!/usr/bin/env python3
"""CLI entry point — see usage below.

Run from project root:
    uv run python scripts/train_vae.py --config configs/vae.yaml
"""

import sys
sys.path.insert(0, "src")

if __name__ == "__main__":
    print("Placeholder script — implement CLI parsing + training loop here.")
