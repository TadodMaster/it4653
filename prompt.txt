You are writing the `README.md` for a small research codebase. I am attaching the source file(s):

- `cvae.py` — the main experiment script

**Ground rule: everything in the README must be derivable from the attached code.** Read the source carefully before writing. Do not invent CLI flags, filenames, default values, dependencies, or results. If something is genuinely ambiguous or missing from the code, add a short `> **Note:**` callout flagging it rather than guessing. Never fabricate benchmark numbers, accuracy figures, loss curves, or training times — where a results table would go, emit an empty table with the correct column headers and a placeholder row.

## Required sections, in this order

1. **Title and one-paragraph summary** — what the project does and who it is for, in plain language. No marketing tone, no emoji, no badges.

2. **Features** — a short bulleted list of what the script actually supports (model variants, loss options, latent-dim sweeps, anomaly-detection experiment, generated artifacts). One line each, derived from the code.

3. **Background / method** — the theory needed to read the code, using LaTeX in `$...$` and `$$...$$`. Cover the latent-variable formulation, the ELBO and its two terms, the reparameterization trick, and how each maps onto a specific function or line in the source (name the functions explicitly, e.g. `kl_divergence`, `reparameterize`). Explain why the reconstruction loss choice (`bce` vs `mse`) corresponds to a likelihood assumption on the pixels. Explain the role of `beta`. Keep this to roughly 400–600 words — enough to orient a reader, not a paper.

4. **Repository structure** — a `tree`-style block showing the source files plus the run-directory layout the script creates at runtime, with a one-line comment on each generated file. Derive the exact filenames and the run-directory naming pattern from the code.

5. **Installation** — Python version and the exact third-party packages imported by the script, as both a `pip install` line and a `requirements.txt` block. Mention that the dataset downloads automatically on first run and note the PyTorch/CUDA install caveat.

6. **Quick start** — three or four copy-pasteable commands: a fast smoke test using the batch-limiting flags, a default full run, and one or two interesting variations (different dataset, latent-dim sweep, beta sweep). Precede each with one line saying what it does and roughly what it costs.

7. **Command-line reference** — a markdown table with columns `Flag | Type | Default | Description`, containing **every** argument defined in the argument parser, in the order they are defined. Descriptions must be accurate and specific (say what the flag changes in the run, not just restate its name).

8. **Outputs** — describe every file the script writes: the CSV logs with their exact column names, each PNG and how to read it, and the checkpoints with a snippet showing how to reload one into the model class for inference.

9. **Experiments** — a subsection per experiment implemented in the code (the latent-dimension/loss sweep, and the leave-one-class-out anomaly detection). For each: what question it answers, how it is set up in the code, which metrics are computed and their exact definitions (including how the threshold and recall-at-threshold are calculated), and what a reader should look for in the results. Include an empty results table with correct headers.

10. **Reproducibility** — what the seeding function controls, the determinism flags that are set, and honest caveats about nondeterminism (GPU/MPS kernels, dataloader workers, cuDNN).

11. **Troubleshooting** — a short table of realistic failure modes with fixes: out-of-memory, MPS/CUDA unavailability, dataloader worker issues, NaN loss, KL collapsing to zero, dataset download failure.

12. **Extending the code** — concrete pointers for likely modifications: adding a new reconstruction loss, swapping the dataset, changing the encoder/decoder architecture, and moving from VAE to conditional VAE. Name the exact functions to touch.

13. **References** — the canonical papers (Kingma & Welling 2013 for the VAE, Higgins et al. 2017 for β-VAE, Sohn et al. 2015 for the CVAE), author/year/title only.

## Style requirements

- GitHub-flavoured Markdown, rendering correctly on GitHub. Use `$$...$$` for display math.
- Sentence case headings. `##` for top-level sections, `###` for subsections.
- Fenced code blocks with language tags (`bash`, `python`, `text`).
- Prose in short paragraphs; use bullets and tables where they genuinely aid scanning, not as a default.
- Reference identifiers from the source in backticks so a reader can grep for them.
- No emoji, no badge images, no "Contributing"/"License" boilerplate unless I ask.
- Assume the reader knows Python and basic deep learning but has never seen this repository.

## Before you finish

Re-read the attached source and verify: every flag in the CLI table exists in the parser with that exact default; every filename in the outputs section is actually written by the code; every CSV column name matches the dictionary keys in the logging rows. List any discrepancies you had to resolve at the very end of your reply, outside the README itself.

Output the complete README as a single `README.md` file.